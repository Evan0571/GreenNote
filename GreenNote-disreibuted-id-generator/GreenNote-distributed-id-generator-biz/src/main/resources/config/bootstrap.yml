spring:
  application:
    name: GreenNote-distributed-id-generator
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: true
        group: DEFAULT_GROUP
        namespace: greennote
        server-addr: 127.0.0.1:8848
